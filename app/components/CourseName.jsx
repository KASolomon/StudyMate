import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Input } from "native-base";

export default function CourseName() {
  return <Input placeholder="Course" w={"60%"} />;
}

const styles = StyleSheet.create({})